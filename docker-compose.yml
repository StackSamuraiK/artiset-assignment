version: '3.8'

services:
  ecommerce:
    build: .
    container_name: ecommerce-app
    ports:
      - "80:80"
    environment:
      - TZ=UTC
    restart: unless-stopped
    volumes:
      - ./:/usr/share/nginx/html
